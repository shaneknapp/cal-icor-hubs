nfsPVC:
  nfs:
    shareName: shares/cuesta/staging

jupyterhub:
  scheduling:
    userScheduler:
      replicas: 1
  prePuller:
    continuous:
      enabled: false
  ingress:
    enabled: true
    hosts:
      - cuesta-staging.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - cuesta-staging.jupyter.cal-icor.org
  hub:
    config:
      CILogonOAuthenticator:
        oauth_callback_url: https://cuesta-staging.jupyter.cal-icor.org/hub/oauth_callback
