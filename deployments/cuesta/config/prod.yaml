nfsPVC:
  nfs:
    shareName: shares/cuesta/prod

jupyterhub:
  ingress:
    enabled: true
    hosts:
      - cuesta.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - cuesta.jupyter.cal-icor.org
  hub:
    db:
      pvc:
        # This also holds logs
        storage: 4Gi
    config:
        CILogonOAuthenticator:
          oauth_callback_url: https://cuesta.jupyter.cal-icor.org/hub/oauth_callback
