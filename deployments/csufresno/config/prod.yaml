nfsPVC:
  nfs:
    shareName: csufresno/prod

jupyterhub:
  ingress:
    enabled: true
    hosts:
      - csufresno.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - csufresno.jupyter.cal-icor.org
  hub:
    db:
      pvc:
        # This also holds logs
        storage: 4Gi
    config:
        CILogonOAuthenticator:
          oauth_callback_url: https://csufresno.jupyter.cal-icor.org/hub/oauth_callback
