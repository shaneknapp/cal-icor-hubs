nfsPVC:
  nfs:
    shareName: shares/gpu-demo/staging

jupyterhub:
  scheduling:
    userScheduler:
      replicas: 1
  prePuller:
    continuous:
      enabled: false
  ingress:
    enabled: true
    hosts:
      - gpu-demo-staging.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - gpu-demo-staging.jupyter.cal-icor.org
  hub:
    config:
      CILogonOAuthenticator:
        oauth_callback_url: https://gpu-demo-staging.jupyter.cal-icor.org/hub/oauth_callback
