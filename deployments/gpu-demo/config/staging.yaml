nfsPVC:
  nfs:
    shareName: shares/gpu-demo/staging

jupyterhub:
  scheduling:
    userScheduler:
      replicas: 1
  prePuller:
    continuous:
      enabled: false
  ingress:
    enabled: true
    hosts:
      - gpu-demo-staging.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - gpu-demo-staging.jupyter.cal-icor.org
