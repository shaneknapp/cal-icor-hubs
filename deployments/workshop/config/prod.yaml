#nfsPVC:
#  nfs:
#    shareName: TBD

jupyterhub:
  ingress:
    enabled: true
    hosts:
      - workshop.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - workshop.jupyter.cal-icor.org

  proxy:
    https:
      hosts:
        - workshop.jupyter.cal-icor.org
  hub:
    db:
      pvc:
        # This also holds logs
        storage: 4Gi
