nfsPVC:
  nfs:
    shareName: csun/prod

jupyterhub:
  ingress:
    enabled: true
    hosts:
      - csun.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - csun.jupyter.cal-icor.org
  hub:
    db:
      pvc:
        # This also holds logs
        storage: 4Gi
    config:
        CILogonOAuthenticator:
          oauth_callback_url: https://csun.jupyter.cal-icor.org/hub/oauth_callback
