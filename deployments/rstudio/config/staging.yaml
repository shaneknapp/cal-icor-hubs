nfsPVC:
  nfs:
    shareName: shares/rstudio/staging

jupyterhub:
  scheduling:
    userScheduler:
      replicas: 1
  prePuller:
    continuous:
      enabled: false
  ingress:
    enabled: true
    hosts:
      - rstudio-staging.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - rstudio-staging.jupyter.cal-icor.org
  hub:
    config:
      CILogonOAuthenticator:
        oauth_callback_url: https://rstudio-staging.jupyter.cal-icor.org/hub/oauth_callback
