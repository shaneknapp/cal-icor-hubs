nfsPVC:
  nfs:
    shareName: shares/jupyter/staging

jupyterhub:
  scheduling:
    userScheduler:
      replicas: 1
  prePuller:
    continuous:
      enabled: false
  ingress:
    enabled: true
    hosts:
      - rstudio-staging.workshop.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - rstudio-staging.workshop.jupyter.cal-icor.org
