nfsPVC:
  nfs:
    shareName: shares/demo/staging

jupyterhub:
  scheduling:
    userScheduler:
      replicas: 1
  prePuller:
    continuous:
      enabled: false
  ingress:
    enabled: true
    hosts:
      - demo-staging.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - demo-staging.jupyter.cal-icor.org
  hub:
    config:
      CILogonOAuthenticator:
        oauth_callback_url: https://demo-staging.jupyter.cal-icor.org/hub/oauth_callback
