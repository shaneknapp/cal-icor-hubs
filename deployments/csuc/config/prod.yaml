nfsPVC:
  nfs:
    shareName: shares/csuc/prod

jupyterhub:
  ingress:
    enabled: true
    hosts:
      - csuc.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - csuc.jupyter.cal-icor.org
  hub:
    db:
      pvc:
        # This also holds logs
        storage: 4Gi
    config:
        CILogonOAuthenticator:
          oauth_callback_url: https://csuc.jupyter.cal-icor.org/hub/oauth_callback
