nfsPVC:
  nfs:
    shareName: shares/folsomlake/prod

jupyterhub:
  ingress:
    enabled: true
    hosts:
      - folsomlake.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - folsomlake.jupyter.cal-icor.org
  hub:
    db:
      pvc:
        # This also holds logs
        storage: 4Gi
    config:
        CILogonOAuthenticator:
          oauth_callback_url: https://folsomlake.jupyter.cal-icor.org/hub/oauth_callback
