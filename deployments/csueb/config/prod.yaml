nfsPVC:
  nfs:
    shareName: shares/csueb/prod

jupyterhub:
  ingress:
    enabled: true
    hosts:
      - csueb.jupyter.cal-icor.org
    tls:
      - secretName: tls-cert
        hosts:
          - csueb.jupyter.cal-icor.org
  hub:
    db:
      pvc:
        # This also holds logs
        storage: 4Gi
    config:
        CILogonOAuthenticator:
          oauth_callback_url: https://csueb.jupyter.cal-icor.org/hub/oauth_callback
